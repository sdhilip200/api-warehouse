# MEMORY — API auth quirks

Append one line per API when the smoke test reveals behavior not covered by
the standard patterns in `references/auth-patterns.md`. Read this file at the
start of Step 2 (Determine the Auth Type) so previously learned quirks apply
automatically. Format: `<API name>: <quirk description>`.

---

## Entries

<!-- Example entry format:
Shopify Admin API: token must be sent as `X-Shopify-Access-Token: <token>`, not as a Bearer header.
-->
